#include <stdio.h>

int main() {
    int n;int rev=0;
    printf("Enter the number:\n");
    scanf("%d",&n);
    
    while(n>0)
    {
        int dig=n%10;
        rev=rev*10+(dig);
        n/=10;
    }
    printf("Reversed number is %d",rev);  
}

