#include <stdio.h>
#include <math.h>
int main() 
{
    int r,area,sq,circum;
    printf("Enter radius of the circle: \n");
    scanf("%d", &r);
    sq = r*r;
    area = M_PI*sq;
    printf("Area of the circle:%d \n",area);
    circum = 2*M_PI*r;
    printf("Circumference of the circle:%d",circum);
}
    
