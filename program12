#include <stdio.h>

int main() {
    int n; int even=0;int odd=0;
    printf("Enter the size of array:\n");
    scanf("%d",&n);
    int a[n];
    int b[n];
    printf("Enter elements of array:\n");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        b[i]=a[i];
    }

    printf("Copied array:");
    for(int i=0;i<n;i++)
    {
        printf("%d ",b[i]);
    }
    
}

