#include <stdio.h>
int recursive(n)
{
    if(n==1) return 1;
    return n+recursive(n-1);
}
int main() {
    int a,sum=0;
	printf("Enter the number:\n");
	scanf("%d",&a);
	sum=recursive(a);
	printf("%d",sum);
}

