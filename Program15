#include <stdio.h>
int fib(n)
{
    if(n==1) return 1;
    if(n==0) return 0;
    return fib(n-1)+fib(n-2);
}
int main() {
    int a,fibonacci;
	printf("Enter the number:\n");
	scanf("%d",&a);
	for(int i=0;i<=a;i++)
	{
	    printf("%d ",fib(i));
	}
}






