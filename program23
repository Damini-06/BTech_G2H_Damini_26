#include <stdio.h>

int main() {
	int n;int sum=0; double avg;
	printf("Enter size of array:\n");
	scanf("%d",&n);
	int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        sum+=a[i];
    }
    
    avg = (double)sum/n;
    
	printf("Sum is:%d and average is:%lf",sum,avg);
}

